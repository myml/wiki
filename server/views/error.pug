extends master.pug

block body
  #root.is-fullscreen
    .app-error
      a(href='/')
        img(src='http://www.deepin.org/wp-content/uploads/2017/01/logo.svg')
      strong= title
      span= message
            
      if error.stack
        pre: code #{error.stack}
